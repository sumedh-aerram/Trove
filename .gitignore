# Env
.env
.env.local
*.env.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
.next/
out/
dist/
*.tsbuildinfo
npm-debug.log*

# Models / caches
.cache/
.hf_cache/

# Crawler runtime state
.crawl_cursor.json
workers/.crawl_cursor.json

# Self-improving loop: runtime usage artifacts + fine-tuned models (regenerated
# locally from each deployment's own traffic; not checked in).
apps/api/eval/*.json
apps/api/models/
apps/api/app/services/ltr_model.bak.txt

# Playwright artifacts
.playwright-mcp/

# OS
.DS_Store
